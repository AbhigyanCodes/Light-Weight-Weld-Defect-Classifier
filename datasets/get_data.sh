#!/usr/bin/env bash
# A small helper to download dataset if you host it somewhere (optional).
# Edit the URL below or replace with your own data portal commands.

set -e
DATA_DIR="data/raw"
mkdir -p "$DATA_DIR"
echo "Downloading dataset (placeholder) — update URL in this script."
# Example: wget -O "$DATA_DIR/dataset.zip" "https://example.com/dataset.zip"
# unzip -o "$DATA_DIR/dataset.zip" -d "$DATA_DIR"
echo "Please replace the placeholder URL with the actual dataset link."
